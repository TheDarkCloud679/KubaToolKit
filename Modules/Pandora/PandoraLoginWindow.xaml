<Window
    x:Class="KubaToolKit.Modules.Pandora.PandoraLoginWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
    Title="Pandora - Connexion"
    Width="900"
    Height="700"
    MinWidth="500"
    MinHeight="400"
    WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <wv2:WebView2 x:Name="WebView" Grid.Row="0"/>

        <Grid Grid.Row="1" Margin="12,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                VerticalAlignment="Center"
                TextWrapping="Wrap"
                Text="Une fois connecté et la console Pandora affichée, clique sur Continuer si la fenêtre ne se ferme pas automatiquement."/>

            <Button
                x:Name="ContinueButton"
                Grid.Column="1"
                Margin="12,0,0,0"
                Padding="14,4"
                Content="Continuer"
                Click="ContinueButton_Click"/>
        </Grid>
    </Grid>
</Window>
