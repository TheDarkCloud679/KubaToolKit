<UserControl
    x:Class="KubaToolKit.Modules.Pandora.PandoraView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid x:Name="ResultsPanel">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Progress -->
        <Grid
            Grid.Row="0"
            Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock
                x:Name="ProgressTextBlock"
                Grid.Column="0"
                Margin="2,0,0,0"
                VerticalAlignment="Center"
                FontSize="11"
                Foreground="{StaticResource TextSecondaryBrush}"/>
            <ProgressBar
                x:Name="LoadingProgressBar"
                Grid.Column="1"
                Margin="16,0,4,0"
                Minimum="0"
                Maximum="100"
                Value="0"
                VerticalAlignment="Center"/>
        </Grid>

        <!-- Groupes Pandora, chacun listant ses agents -->
        <Border
            Grid.Row="1"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="{StaticResource RadiusMedium}"
            Background="{StaticResource SurfaceBrush}"
            Effect="{StaticResource CardShadowEffect}"
            Padding="{StaticResource CardPadding}">
            <ScrollViewer
                VerticalScrollBarVisibility="Auto"
                HorizontalScrollBarVisibility="Disabled"
                CanContentScroll="True">
                <ItemsControl x:Name="GroupsItemsControl">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Expander
                                IsExpanded="False"
                                Margin="0,0,0,8">
                                <Expander.Header>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock
                                            Text="{Binding Name}"
                                            FontWeight="Bold"
                                            FontSize="13"
                                            Foreground="{StaticResource AccentBrush}"/>
                                        <Border
                                            Margin="8,0,0,0"
                                            Padding="7,1"
                                            CornerRadius="8"
                                            Background="{StaticResource AccentSoftBrush}"
                                            VerticalAlignment="Center">
                                            <TextBlock
                                                FontSize="11"
                                                FontWeight="SemiBold"
                                                Foreground="{StaticResource TextSecondaryBrush}"
                                                Text="{Binding Count, StringFormat=({0} agents)}"/>
                                        </Border>
                                    </StackPanel>
                                </Expander.Header>
                                <DataGrid
                                    ItemsSource="{Binding Agents}"
                                    AutoGenerateColumns="False"
                                    IsReadOnly="True"
                                    HeadersVisibility="Column"
                                    CanUserAddRows="False"
                                    CanUserDeleteRows="False"
                                    Margin="0,6,0,0"
                                    MaxHeight="350"
                                    MouseDoubleClick="AgentsDataGrid_DoubleClick"
                                    HorizontalScrollBarVisibility="Visible">
                                    <DataGrid.Columns>
                                        <DataGridTemplateColumn
                                            Header="Status"
                                            Width="Auto">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Border
                                                        Padding="7,1"
                                                        CornerRadius="8"
                                                        Background="{Binding StatusBrush}"
                                                        HorizontalAlignment="Left"
                                                        VerticalAlignment="Center">
                                                        <TextBlock
                                                            FontSize="11"
                                                            FontWeight="SemiBold"
                                                            Foreground="White"
                                                            Text="{Binding StatusLabel}"/>
                                                    </Border>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTextColumn
                                            Header="Agent"
                                            Binding="{Binding Alias}"
                                            Width="Auto"/>
                                        <DataGridTextColumn
                                            Header="Address"
                                            Binding="{Binding Address}"
                                            Width="Auto"/>
                                        <DataGridTextColumn
                                            Header="OS"
                                            Binding="{Binding OsName}"
                                            Width="Auto"/>
                                        <DataGridTemplateColumn
                                            Header="Comments"
                                            Width="*">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBlock
                                                        Text="{Binding Comments}"
                                                        TextWrapping="Wrap"
                                                        TextTrimming="CharacterEllipsis"
                                                        VerticalAlignment="Center"
                                                        MaxHeight="42"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Expander>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
