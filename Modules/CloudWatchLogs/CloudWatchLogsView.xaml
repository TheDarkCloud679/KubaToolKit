<UserControl
    x:Class="KubaToolKit.Modules.CloudWatchLogs.CloudWatchLogsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit">
    <Grid>
        <Grid.RowDefinitions>

            <!-- Log groups -->
            <RowDefinition Height="300"
                   MinHeight="80"/>

            <!-- Splitter -->
            <RowDefinition Height="5"/>

            <!-- Debug -->
            <RowDefinition Height="150"
               MinHeight="80"/>

            <!-- Splitter -->
            <RowDefinition Height="5"/>

            <!-- Results -->
            <RowDefinition Height="*"
                   MinHeight="150"/>
        </Grid.RowDefinitions>

        <!-- ===================================================== -->
        <!-- LOG GROUPS -->
        <!-- ===================================================== -->

        <Border
            x:Name="LogGroupsPanel"
            Grid.Row="0"
            Padding="8"
            Margin="0,0,0,6"
            BorderBrush="#DADADA"
            BorderThickness="1"
            CornerRadius="4">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock
                    Grid.Row="0"
                    Margin="0,-4,0,10"
                    FontWeight="Bold"
                    FontSize="12"
                    Text="Log Groups"/>
                <CheckBox
                    x:Name="SearchAllLogsCheckBox"
                    Margin="0,18,0,10"
                    Content="Search in all log groups"
                    FontWeight="SemiBold"
                    Checked="SearchAllLogsCheckBox_Changed"
                    Unchecked="SearchAllLogsCheckBox_Changed" Grid.RowSpan="2"/>
                <TreeView
    x:Name="LogGroupsTreeView"
    Grid.Row="2"
    PreviewMouseWheel="LogGroupsTreeView_PreviewMouseWheel">

                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate
        ItemsSource="{Binding Children}">

                            <StackPanel
            Orientation="Horizontal">

                                <CheckBox
                IsChecked="{Binding IsChecked, Mode=TwoWay}"
                VerticalAlignment="Center"/>

                                <TextBlock
                Text="{Binding Name}"
                Margin="6,0,0,0"
                VerticalAlignment="Center"/>
                            </StackPanel>
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </Grid>
        </Border>
        <GridSplitter
               x:Name="LogGroupSplitter"
               Grid.Row="1"
               Height="5"
               HorizontalAlignment="Stretch"
               VerticalAlignment="Center"
               Background="#D0D0D0"
               ResizeBehavior="PreviousAndNext"
               ResizeDirection="Rows"/>

        <!-- ===================================================== -->
        <!-- DEBUG MODE -->
        <!-- ===================================================== -->

        <Border
           x:Name="DebugModePanel"
           Grid.Row="2"
           Margin="0,0,0,6"
           Padding="8"
           BorderBrush="#DADADA"
           BorderThickness="1"
           CornerRadius="4">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <CheckBox
                    Grid.Row="0"
                    x:Name="DebugModeCheckBox"
                    Content="Debug mode"
                    FontWeight="SemiBold"
                    Checked="DebugModeCheckBox_Checked"
                    Unchecked="DebugModeCheckBox_Unchecked"/>
                <Grid
                   Grid.Row="1"
                   x:Name="DebugPanel"
                   Visibility="Collapsed"
                   Margin="0,8,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock
                        Grid.Row="0"
                        Margin="0,0,0,4"
                        FontWeight="SemiBold"
                        Text="CloudWatch Logs Insights Query"/>
                    <avalonedit:TextEditor
                        x:Name="QueryEditorTextBox"
                        Grid.Row="1"
                        MinHeight="120"
                        FontFamily="Consolas"
                        FontSize="13"
                        ShowLineNumbers="True"
                        HorizontalScrollBarVisibility="Auto"
                        VerticalScrollBarVisibility="Auto"/>
                    <Grid
                      Grid.Row="2"
                      Margin="0,8,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button
                            Grid.Column="0"
                            Width="120"
                            Height="30"
                            Content="Preview Query"
                            Click="PreviewQuery_Click"/>
                        <Button
                            Grid.Column="2"
                            Width="120"
                            Height="30"
                            Content="Copy Query"
                            Click="CopyQuery_Click"/>
                        <Button
                            Grid.Column="4"
                            Width="150"
                            Height="30"
                            Content="Use Custom Query"
                            Click="UseCustomQuery_Click"/>
                    </Grid>
                </Grid>
            </Grid>
        </Border>
        <GridSplitter
               x:Name="DebugSplitter"
               Grid.Row="3"
               Height="5"
               HorizontalAlignment="Stretch"
               VerticalAlignment="Center"
               Background="#D0D0D0"
               ResizeBehavior="PreviousAndNext"
               ResizeDirection="Rows"/>

        <!-- ===================================================== -->
        <!-- RESULTS -->
        <!-- ===================================================== -->

        <Grid Grid.Row="4"
            x:Name="ResultsPanel">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Progress -->
            <Grid
              Grid.Row="0"
              Margin="0,0,0,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="140"/>
                </Grid.ColumnDefinitions>
                <TextBlock
                    x:Name="ProgressTextBlock"
                    Grid.Column="0"
                    Margin="6,0,0,0"
                    VerticalAlignment="Center"
                    FontSize="11"
                    Foreground="Gray"/>
                <ProgressBar
                    x:Name="SearchProgressBar"
                    Grid.Column="1"
                    Height="6"
                    Minimum="0"
                    Maximum="100"
                    Value="0"
                    VerticalAlignment="Center"
                    Foreground="#4CAF50"
                    Background="#D9D9D9"/>
            </Grid>

            <!-- Results -->
            <Border
                 Grid.Row="1"
                 BorderBrush="#DADADA"
                 BorderThickness="1"
                 CornerRadius="4"
                 Padding="4">
                <ScrollViewer
                        VerticalScrollBarVisibility="Auto"
                        HorizontalScrollBarVisibility="Disabled"
                        CanContentScroll="True">
                    <ItemsControl
                       x:Name="LogsGroupedItemsControl">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Expander
                                    IsExpanded="False"
                                    Margin="0,0,0,6">
                                    <Expander.Header>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock
                                                Text="{Binding LogGroup}"
                                                FontWeight="Bold"
                                                Foreground="#204080"/>
                                            <TextBlock
                                                Margin="8,0,0,0"
                                                Foreground="Gray"
                                                Text="{Binding Count, StringFormat=({0} logs)}"/>
                                        </StackPanel>
                                    </Expander.Header>
                                    <DataGrid
                                            ItemsSource="{Binding Logs}"
                                            AutoGenerateColumns="False"
                                            IsReadOnly="True"
                                            HeadersVisibility="Column"
                                            CanUserAddRows="False"
                                            CanUserDeleteRows="False"
                                            Margin="0,4,0,0"
                                            MaxHeight="350"
                                            FontSize="12"
                                            MouseDoubleClick="LogsDataGrid_DoubleClick"
                                            HorizontalScrollBarVisibility="Visible">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn
                                                Header="Timestamp"
                                                Binding="{Binding Timestamp}"
                                                Width="180"/>
                                            <DataGridTemplateColumn
                                                    Header="Message"
                                                    Width="*">
                                                <DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <TextBlock
                                                            Text="{Binding Message}"
                                                            TextWrapping="Wrap"
                                                            TextTrimming="CharacterEllipsis"
                                                            VerticalAlignment="Center"
                                                            MaxHeight="42"/>
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                            </DataGridTemplateColumn>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </Expander>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>
