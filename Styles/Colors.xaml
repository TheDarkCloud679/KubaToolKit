<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- =====================================================
         PALETTE
         ===================================================== -->

    <!-- "Signal" palette: a teal-forward accent (distinct from the sea of
         generic SaaS blues) on genuinely neutral grays (no blue undertone
         competing with the accent), for a more deliberate, less
         default-Windows-Forms identity. -->

    <Color x:Key="WindowBackgroundColor">#F4F4F5</Color>
    <Color x:Key="SurfaceColor">#FFFFFF</Color>
    <Color x:Key="SurfaceAltColor">#F7F7F8</Color>
    <Color x:Key="HeaderColor">#FFFFFF</Color>

    <Color x:Key="BorderColor">#E3E3E6</Color>
    <Color x:Key="BorderStrongColor">#CBCBD1</Color>

    <Color x:Key="TextPrimaryColor">#16181D</Color>
    <Color x:Key="TextSecondaryColor">#5C6068</Color>
    <Color x:Key="TextMutedColor">#8B8F98</Color>
    <Color x:Key="TextOnAccentColor">#FFFFFF</Color>

    <Color x:Key="AccentColor">#0C8599</Color>
    <Color x:Key="AccentHoverColor">#0A7186</Color>
    <Color x:Key="AccentPressedColor">#085C6E</Color>
    <Color x:Key="AccentSoftColor">#E3F4F6</Color>
    <Color x:Key="AccentSoftHoverColor">#CDEBEF</Color>

    <Color x:Key="SuccessColor">#1E9E6B</Color>
    <Color x:Key="DangerColor">#E5484D</Color>
    <Color x:Key="DangerHoverColor">#D33D42</Color>
    <Color x:Key="WarningColor">#F2A93B</Color>

    <!-- Warm counterpoint to the teal accent, used sparingly (brand
         mark, occasional highlight), never for state/status so it can't
         be mistaken for success/warning/danger. -->
    <Color x:Key="AccentSecondaryColor">#FF6B4A</Color>

    <!-- =====================================================
         BRUSHES
         ===================================================== -->

    <SolidColorBrush x:Key="WindowBackgroundBrush" Color="{StaticResource WindowBackgroundColor}"/>
    <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}"/>
    <SolidColorBrush x:Key="SurfaceAltBrush" Color="{StaticResource SurfaceAltColor}"/>
    <SolidColorBrush x:Key="HeaderBrush" Color="{StaticResource HeaderColor}"/>

    <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}"/>
    <SolidColorBrush x:Key="BorderStrongBrush" Color="{StaticResource BorderStrongColor}"/>

    <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}"/>
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}"/>
    <SolidColorBrush x:Key="TextMutedBrush" Color="{StaticResource TextMutedColor}"/>
    <SolidColorBrush x:Key="TextOnAccentBrush" Color="{StaticResource TextOnAccentColor}"/>

    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
    <SolidColorBrush x:Key="AccentHoverBrush" Color="{StaticResource AccentHoverColor}"/>
    <SolidColorBrush x:Key="AccentPressedBrush" Color="{StaticResource AccentPressedColor}"/>
    <SolidColorBrush x:Key="AccentSoftBrush" Color="{StaticResource AccentSoftColor}"/>
    <SolidColorBrush x:Key="AccentSoftHoverBrush" Color="{StaticResource AccentSoftHoverColor}"/>

    <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}"/>
    <SolidColorBrush x:Key="DangerBrush" Color="{StaticResource DangerColor}"/>
    <SolidColorBrush x:Key="DangerHoverBrush" Color="{StaticResource DangerHoverColor}"/>
    <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}"/>
    <SolidColorBrush x:Key="AccentSecondaryBrush" Color="{StaticResource AccentSecondaryColor}"/>

    <LinearGradientBrush x:Key="AccentGradientBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="{StaticResource AccentColor}" Offset="0"/>
        <GradientStop Color="{StaticResource AccentPressedColor}" Offset="1"/>
    </LinearGradientBrush>

    <!-- The brand mark's own gradient, teal into the warm secondary,
         kept off every other control so it reads as "the logo", not "the
         button color". -->
    <LinearGradientBrush x:Key="BrandGradientBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="{StaticResource AccentColor}" Offset="0"/>
        <GradientStop Color="{StaticResource AccentSecondaryColor}" Offset="1"/>
    </LinearGradientBrush>

    <!-- Native selection colors (TreeView / ListBox / ComboBoxItem) recolored
         so the built-in highlight chrome matches the accent palette instead of
         the harsh default system blue. -->
    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="{StaticResource AccentSoftColor}"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="{StaticResource TextPrimaryColor}"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="{StaticResource AccentSoftColor}"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="{StaticResource TextPrimaryColor}"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="{StaticResource AccentSoftColor}"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="{StaticResource TextPrimaryColor}"/>

    <!-- =====================================================
         EFFECTS
         ===================================================== -->

    <DropShadowEffect
        x:Key="CardShadowEffect"
        Color="{StaticResource TextPrimaryColor}"
        Direction="270"
        ShadowDepth="2"
        BlurRadius="16"
        Opacity="0.09"/>

</ResourceDictionary>
