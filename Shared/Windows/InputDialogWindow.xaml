<Window x:Class="KubaToolKit.Shared.Windows.InputDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rename"
        Width="460"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{StaticResource SurfaceBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock
            x:Name="PromptText"
            Grid.Row="0"
            Margin="0,0,0,10"
            FontWeight="SemiBold"
            FontSize="13"
            Foreground="{StaticResource TextPrimaryBrush}"/>

        <TextBox
            x:Name="ValueTextBox"
            Grid.Row="1"
            Height="34"
            VerticalContentAlignment="Center"/>

        <StackPanel
            Grid.Row="2"
            Orientation="Horizontal"
            HorizontalAlignment="Right"
            Margin="0,20,0,0">
            <Button
                Content="Cancel"
                Width="90"
                IsCancel="True"/>
            <Button
                x:Name="OkButton"
                Width="90"
                Margin="10,0,0,0"
                Style="{StaticResource AccentButtonStyle}"
                IsDefault="True"
                Click="OkButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
