/.vs
/bin
/obj

# Personal AWS SSO reference files (see Config/README.md)
Config/config
Config/credentials

# Pandora FMS profiles (URL + login per site) -- contains credentials
Config/pandora-profiles.json

# Per-user Visual Studio project settings (machine-specific paths)
*.csproj.user
